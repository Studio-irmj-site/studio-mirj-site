(()=>{'use strict';const cfg=window.SUPABASE_CONFIG||{};if(!window.supabase||!cfg.url||!cfg.anonKey)return;const db=window.supabase.createClient(cfg.url,cfg.anonKey);let checking=false;document.addEventListener('submit',async e=>{if(e.target?.id!=='appointmentForm'||checking)return;e.preventDefault();e.stopImmediatePropagation();const form=e.target,dateValue=document.getElementById('pDate')?.value,date=new Date(dateValue);if(Number.isNaN(date.getTime()))return;const editing=form.dataset.editingId||null,start=new Date(date.getTime()-29*60000).toISOString(),end=new Date(date.getTime()+29*60000).toISOString();let q=db.from('appointments').select('id,client_name,appointment_at,status').gte('appointment_at',start).lte('appointment_at',end).not('status','eq','cancelado');if(editing)q=q.neq('id',editing);const {data,error}=await q;if(error){const box=document.getElementById('appointmentError');if(box)box.textContent='Não foi possível verificar conflitos de horário.';return}if(data?.length){const box=document.getElementById('appointmentError');if(box)box.textContent='Já existe um agendamento próximo desse horário. Escolha outro horário.';return}checking=true;form.requestSubmit();setTimeout(()=>{checking=false},0)},true);})();