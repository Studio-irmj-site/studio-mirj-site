(()=>{'use strict';const cfg=window.SUPABASE_CONFIG||{};if(!window.supabase||!cfg.url||!cfg.anonKey)return;const db=window.supabase.createClient(cfg.url,cfg.anonKey);const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function range(kind){const n=new Date();if(kind==='today'){const s=new Date(n.getFullYear(),n.getMonth(),n.getDate());return [s,new Date(s.getTime()+86400000)]}if(kind==='7'){const s=new Date(n);s.setDate(s.getDate()-6);s.setHours(0,0,0,0);return [s,new Date(n.getFullYear(),n.getMonth(),n.getDate()+1)]}if(kind==='prev')return [new Date(n.getFullYear(),n.getMonth()-1,1),new Date(n.getFullYear(),n.getMonth(),1)];return [new Date(n.getFullYear(),n.getMonth(),1),new Date(n.getFullYear(),n.getMonth()+1,1)]}let selected='month',busy=false;async function refreshRevenue(){if(busy)return;const title=document.getElementById('title'),content=document.getElementById('content');if(!title||title.textContent!=='Dashboard'||!content)return;busy=true;try{const [start,end]=range(selected),[a,r]=await Promise.all([db.from('attendances').select('amount,attended_at,service_name,payment_method').gte('attended_at',start.toISOString()).lt('attended_at',end.toISOString()),db.from('appointments').select('id,amount,appointment_at,status,service_name').eq('status','concluido').gte('appointment_at',start.toISOString()).lt('appointment_at',end.toISOString())]);if(a.error||r.error)return;const all=(a.data||[]).concat((r.data||[]).map(x=>({amount:x.amount,attended_at:x.appointment_at,service_name:x.service_name,payment_method:'Agenda'}))),revenue=all.reduce((s,x)=>s+Number(x.amount||0),0),count=all.length;document.querySelector('.stats .stat:nth-child(1) strong')?.replaceChildren(document.createTextNode(money(revenue)));document.querySelector('.stats .stat:nth-child(2) strong')?.replaceChildren(document.createTextNode(String(count)));document.querySelector('.stats .stat:nth-child(3) strong')?.replaceChildren(document.createTextNode(money(count?revenue/count:0)));const byDay={},byPay={},byService={};all.forEach(x=>{const d=new Date(x.attended_at),day=String(d.getDate()).padStart(2,'0')+'/'+String(d.getMonth()+1).padStart(2,'0'),amt=Number(x.amount||0);byDay[day]=(byDay[day]||0)+amt;const pay=x.payment_method||'Não informado';byPay[pay]=(byPay[pay]||0)+amt;const svc=x.service_name||'Serviço';byService[svc]=(byService[svc]||0)+1});const days=Object.entries(byDay).sort(),payments=Object.entries(byPay).sort((a,b)=>b[1]-a[1]),services=Object.entries(byService).sort((a,b)=>b[1]-a[1]);let wrap=document.getElementById('revenueDetails');if(!wrap){wrap=document.createElement('div');wrap.id='revenueDetails';content.appendChild(wrap)}wrap.innerHTML='<div class="card filter-card"><div class="filter-head"><div><h3>Período do faturamento</h3><p>Escolha o período que deseja analisar.</p></div><select id="revenuePeriod"><option value="today">Hoje</option><option value="7">Últimos 7 dias</option><option value="month">Este mês</option><option value="prev">Mês anterior</option></select></div></div><div class="dashboard-grid"><div class="card"><h3>Faturamento por dia</h3><div class="mini-bars">'+(days.length?days.map(([d,v])=>'<div class="bar-row"><span>'+d+'</span><div class="bar"><i style="width:'+Math.min(100,v/Math.max(...days.map(x=>x[1]))*100)+'%"></i></div><strong>'+money(v)+'</strong></div>').join(''):'<p class="empty">Nenhum faturamento no período.</p>')+'</div></div><div class="card"><h3>Formas de pagamento</h3><div class="rank-list">'+(payments.length?payments.map(([k,v])=>'<div><span>'+esc(k)+'</span><strong>'+money(v)+'</strong></div>').join(''):'<p class="empty">Nenhum pagamento registrado.</p>')+'</div></div><div class="card"><h3>Serviços mais realizados</h3><div class="rank-list">'+(services.length?services.map(([k,v])=>'<div><span>'+esc(k)+'</span><strong>'+v+' atendimento'+(v===1?'':'s')+'</strong></div>').join(''):'<p class="empty">Nenhum serviço realizado.</p>')+'</div></div></div>';const sel=document.getElementById('revenuePeriod');if(sel){sel.value=selected;sel.onchange=()=>{selected=sel.value;refreshRevenue()}}}finally{busy=false}}const content=document.getElementById('content');if(content)new MutationObserver(()=>{if(document.getElementById('title')?.textContent==='Dashboard'&&!document.getElementById('revenueDetails'))setTimeout(refreshRevenue,50)}).observe(content,{childList:true});window.addEventListener('load',()=>setTimeout(refreshRevenue,700));})();